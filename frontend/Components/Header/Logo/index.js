import React, { Component } from 'react';
import styles from './styles';

const Logo = () => {
  return (
    <div className={styles.logoContainer}>
      <div className={styles.logoTitle}>Welcome to Betterdep!</div>
    </div>
  );
};

export default Logo;
